import SwiftUI

struct AppTypography {
    let headingL = Font.largeTitle.weight(.bold)
    let headingM = Font.title.weight(.semibold)
    let headingS = Font.title3.weight(.medium)
    let bodyL = Font.body
    let bodyM = Font.callout
    let caption = Font.caption
}
